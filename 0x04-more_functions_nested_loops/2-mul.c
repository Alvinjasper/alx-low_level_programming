#include "main.h"

/**
 * mul -  checks for checks for a digit (0 through 9)
 * @a: first int
 * @b: second int
 * Return: the multiplication of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}

